# zip-drain
first ever apt package .deb

What the zip-drain package does:

It can extract your zip or rar files so fast depending on CPU.
It is the first ever unzipper or unrar that deletes file/folders inside the zip/rar once they are fully extracted.
Only available on Ubuntu or any other distro on linux.


how to crack open so that i can prove it isnt a virus!
sudo dpkg-deb -x zip-drain-pkg.deb ~/Downloads to get its code!
